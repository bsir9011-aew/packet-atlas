# Release Readiness Snapshot

Generate a human-readable project state report:

```bash
npm run release:readiness
```

The report distinguishes between:

- application/tooling health,
- manifest and fixture coverage,
- the still-pending first verified real capture milestone.

It writes:

```text
reports/release-readiness.md
```
