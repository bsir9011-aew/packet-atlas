# Packet Atlas v6.3 — Real Capture Timeline

v6.3 compresses the verified real capture into a small evidence timeline.

The timeline groups individual frames into:

- DNS lookup,
- TCP transport setup / maintenance,
- TLS protection layer.

This does not replace frame-level evidence. It gives the user a fast “shape of the trace” before they inspect individual frames.
