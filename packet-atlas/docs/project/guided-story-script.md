# Guided Story Script

This document captures the intended reading voice of Packet Atlas.

The core pattern is:

```text
A human action becomes a browser task.
The browser creates a network question.
The network creates a transport path.
The security layer creates a protected envelope.
The application asks and answers.
The browser renders visible content.
```

The screen should guide the user through that story without requiring them to inspect every panel.
