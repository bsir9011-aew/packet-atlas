# Repository hygiene rules

Packet Atlas should stay centered around one journey and organized workspaces.

Use:

```bash
npm run hygiene:audit
```

The audit is conservative. It reports probable cleanup targets instead of deleting files automatically.
