export type CaptureGuideStep = { id: string; label: string; lookFor: string; trap: string }
export type CaptureWorkspaceGuideSummary = { title: string; promise: string; steps: CaptureGuideStep[]; safetyRule: string }
export function buildCaptureWorkspaceGuideSummary(): CaptureWorkspaceGuideSummary { return { title:'How to read the Capture workspace', promise:'This workspace connects the educational atlas to reviewed real packet evidence.', safetyRule:'Raw PCAP stays local. The app shows only reviewed, normalized and redacted JSON fixtures.', steps:[ {id:'contrast',label:'1. Start with contrast',lookFor:'HTTPS readable HTTP = 0 versus HTTP local readable HTTP > 0.',trap:'Do not start from individual frames before you know the claim.'}, {id:'evidence',label:'2. Read the evidence summary',lookFor:'Frame counts, TLS presence, HTTP visibility and redaction status.',trap:'Counts are evidence, not the whole interpretation.'}, {id:'timeline',label:'3. Compress frames into a timeline',lookFor:'DNS → TCP → TLS for HTTPS and TCP/HTTP for local plaintext.',trap:'Timeline is a teaching lens, not raw packet order for every detail.'}, {id:'inspector',label:'4. Use inspector last',lookFor:'Exact frame details only after the story is clear.',trap:'Packet details without context create noise.'} ] } }
