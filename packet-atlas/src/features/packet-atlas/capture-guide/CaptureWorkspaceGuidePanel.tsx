import { buildCaptureWorkspaceGuideSummary } from './captureWorkspaceGuideModel'
export function CaptureWorkspaceGuidePanel(){const guide=buildCaptureWorkspaceGuideSummary();return <section className="capture-guide-panel" aria-label="Capture workspace guide"><div className="capture-guide__hero"><div><p className="capture-guide__eyebrow">Read this first</p><h3>{guide.title}</h3><p>{guide.promise}</p></div><strong>🦈 evidence mode</strong></div><div className="capture-guide__steps">{guide.steps.map((step)=><article key={step.id}><h4>{step.label}</h4><p><b>Look for:</b> {step.lookFor}</p><small>⚠️ {step.trap}</small></article>)}</div><div className="capture-guide__safety">🔐 {guide.safetyRule}</div></section>}
