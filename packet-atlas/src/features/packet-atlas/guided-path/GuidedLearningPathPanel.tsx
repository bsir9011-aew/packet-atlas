import type { AtlasWorkspaceDefinition } from '../workspace/workspaceModel'
import { buildGuidedLearningPathSummary } from './guidedLearningPathModel'
type Props = { currentWorkspace: AtlasWorkspaceDefinition }
export function GuidedLearningPathPanel({ currentWorkspace }: Props) { const summary=buildGuidedLearningPathSummary(currentWorkspace); return <section className="guided-path-panel" aria-label="Guided learning path"><div className="guided-path__header"><div><p className="guided-path__eyebrow">Learning route</p><h3>{summary.title}</h3><p>{summary.rule}</p></div><div className="guided-path__active"><span>Now</span><strong>{summary.activeStep.label}</strong></div></div><div className="guided-path__steps">{summary.steps.map((step)=><article key={step.id} className={step.id===summary.activeStep.id?'guided-path__step guided-path__step--active':'guided-path__step'}><strong>{step.label}</strong><p>{step.checkpoint}</p><small>✍️ {step.notebookPrompt}</small></article>)}</div></section> }
